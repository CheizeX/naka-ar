import { Dispatch, FC, SetStateAction } from "react";
import styled from "styled-components";
import { StyledContainer } from "./firstSection.styled";

export const FirstSection: FC = () => {
  return <StyledContainer></StyledContainer>;
};
